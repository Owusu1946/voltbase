{
  "name": "voltbase",
  "private": true,
  "scripts": {
    "build:packages": "pnpm --filter @voltbase/constants --filter @voltbase/types run build",
    "dev:api": "pnpm run build:packages && pnpm --filter api run start:dev",
    "dev:web": "pnpm --filter web run dev",
    "dev": "pnpm run build:packages && concurrently \"pnpm --filter api run start:dev\" \"pnpm run dev:web\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
